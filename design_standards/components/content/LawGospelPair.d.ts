export interface LawGospelPairProps {
  /** What the text demands, accuses, exposes. */
  law: React.ReactNode;
  /** What God gives, promises, does. */
  gospel: React.ReactNode;
}

export declare function LawGospelPair(props: LawGospelPairProps): JSX.Element;
