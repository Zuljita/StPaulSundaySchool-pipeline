The Gospel set as running prose in two columns with a hairline column rule and red superscript verse numbers.

```jsx
<Scripture verses={[{ n: 0, text: '…' }]} />
```

Never one verse per line, never a verse-number list. 12px is the floor; shrink other blocks first.
