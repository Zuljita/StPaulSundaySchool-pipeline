The uppercase Montserrat label that opens every block on every sheet.

```jsx
<SectionLabel>What You Need</SectionLabel>
<SectionLabel tone="onSolid">Memory Work</SectionLabel>
```

10px on the page, 9px inside a tint panel. Never sentence case, never in the body face.
