export interface RunningHeaderProps {
  /** Same chip text as page 1, at small size. */
  chip: string;
  /** Right-hand line, usually date and season. */
  right: string;
}

export declare function RunningHeader(props: RunningHeaderProps): JSX.Element;
